name: checkup_app
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  date_field: ^3.0.5
  flutter:
    sdk: flutter
  image_picker: ^1.0.1
  intl: ^0.18.1
  path: ^1.8.3
  share_plus: ^7.0.2
  sqflite: ^2.2.8+4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
